<style>
    /* ====== FOOTER THEME VARIABLES ====== */
    :root {
        /* Light Mode (Default) */
        --ft-bg: #f5f5f5;
        --ft-text: #555555;
        --ft-head: #000000;
        --ft-border: #e0e0e0;
        --ft-input-bg: #ffffff;
        --ft-accent: #000000; /* Black hover in light mode */
    }

    /* Dark Mode Override */
    body.dark-mode {
        --ft-bg: #000000;
        --ft-text: #a0a0a0;
        --ft-head: #ffffff;
        --ft-border: #222222;
        --ft-input-bg: #1a1a1a;
        --ft-accent: #e2c055; /* Gold hover in dark mode */
    }

    /* ====== FOOTER STYLES ====== */
    .footer {
        background-color: var(--ft-bg);
        color: var(--ft-text);
        font-family: 'Inter', sans-serif;
        font-size: 14px;
        border-top: 1px solid var(--ft-border);
        transition: background 0.3s, color 0.3s;
        margin-top: auto; /* Pushes footer to bottom if page is short */
    }

    .footer-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 40px 20px 20px;
    }

    .footer-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr); /* 4 Columns */
        gap: 30px;
        margin-bottom: 40px;
    }

    /* Column Headings */
    .footer-col h3 {
        font-family: 'Tenor Sans', sans-serif;
        color: var(--ft-head);
        font-size: 18px;
        margin-bottom: 15px;
        font-weight: 400;
        letter-spacing: 0.5px;
    }

    /* Links & Text */
    .footer-col p, 
    .footer-col a {
        display: block;
        margin-bottom: 8px;
        color: var(--ft-text);
        text-decoration: none;
        transition: 0.2s;
        line-height: 1.5;
    }

    .footer-col a:hover {
        color: var(--ft-accent);
        padding-left: 3px; /* Subtle movement on hover */
    }

    /* Subscribe Input */
    .subscribe-box {
        position: relative;
        margin-top: 15px;
    }

    .subscribe-input {
        width: 100%;
        padding: 10px 40px 10px 15px;
        background: var(--ft-input-bg);
        border: 1px solid var(--ft-border);
        color: var(--ft-head);
        border-radius: 4px;
        font-size: 13px;
    }
    .subscribe-input:focus {
        outline: none;
        border-color: var(--ft-accent);
    }

    .subscribe-btn {
        position: absolute;
        right: 10px;
        top: 50%;
        transform: translateY(-50%);
        background: none;
        border: none;
        color: var(--ft-text);
        cursor: pointer;
    }
    .subscribe-btn:hover { color: var(--ft-accent); }

    /* Copyright Area */
    .copyright-area {
        border-top: 1px solid var(--ft-border);
        text-align: center;
        padding-top: 20px;
        padding-bottom: 10px;
        font-size: 12px;
        color: var(--ft-text);
        opacity: 0.8;
    }

    /* ====== RESPONSIVE ====== */
    @media (max-width: 992px) {
        .footer-grid {
            grid-template-columns: repeat(2, 1fr); /* 2 Columns on Tablet */
            gap: 40px 20px;
        }
    }

    @media (max-width: 576px) {
        .footer-grid {
            grid-template-columns: 1fr; /* 1 Column on Mobile */
            gap: 30px;
            text-align: center;
        }
        
        .subscribe-input { text-align: center; padding: 10px; }
        .subscribe-btn { right: 15px; }
    }
</style>

<footer class="footer">
    <div class="footer-container">
        
        <div class="footer-grid">
            <div class="footer-col">
                <h3>Sentique</h3>
                <p>Subscribe for exclusive offers.</p>
                <p>Get 10% off your first order.</p>
                <div class="subscribe-box">
                    <input type="email" class="subscribe-input" placeholder="Enter your email">
                    <button class="subscribe-btn"><i class="fas fa-arrow-right"></i></button>
                </div>
            </div>

            <div class="footer-col">
                <h3>Support</h3>
                <p>Muhammed Shamil, Kerala, India</p>
                <a href="mailto:exclusive@gmail.com">sentiqueexclusive@gmail.com</a>
                <a href="tel:919876543210">+919876543210</a>
            </div>

            <div class="footer-col">
                <h3>Account</h3>
                <a href="/profile">My Account</a>
                <a href="/login">Login / Register</a>
                <a href="/cart">Cart</a>
                <a href="/wishlist">Wishlist</a>
            </div>

            <div class="footer-col">
                <h3>Quick Links</h3>
                <a href="/privacy">Privacy Policy</a>
                <a href="/terms">Terms Of Use</a>
                <a href="/faq">FAQ</a>
                <a href="/contact">Contact</a>
            </div>
        </div>

        <div class="copyright-area">
            &copy; Copyright Sentique <span id="copyright-year"></span>. All rights reserved.
        </div>
    </div>
</footer>

<script>
    // Automatically set the current year
    document.getElementById('copyright-year').textContent = new Date().getFullYear();
</script>