<%- include("../partials/user/header.ejs") %>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Verification - Sentique</title>

    <style>
        /* --- Dark/Light Mode Variables for Content --- */
        :root {
            --card-bg: #ffffff;
            --input-bg: #f9fafb;
            --input-border: #d1d5db;
            --subtitle-color: #6b7280;
            --timer-bg: #fef2f2;
            --timer-border: #fecaca;
            --timer-text: #ef4444;
            --input-filled-bg: #ecfdf5;
            --input-filled-border: #059669;
        }

        body.dark-mode {
            --card-bg: #1a1a1a;
            --input-bg: #252525;
            --input-border: #333333;
            --subtitle-color: #a0a0a0;
            --timer-bg: rgba(239, 68, 68, 0.1);
            --timer-border: rgba(239, 68, 68, 0.2);
            --timer-text: #f87171;
            --input-filled-bg: rgba(5, 150, 105, 0.1);
            --input-filled-border: #10b981;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--bg-body) !important;
            color: var(--text-main) !important;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .main-content {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px 20px;
        }

        .otp-container {
            background-color: var(--card-bg);
            border-radius: 16px;
            box-shadow: var(--shadow);
            padding: 50px 40px;
            width: 100%;
            max-width: 450px;
            text-align: center;
            border: 1px solid var(--border-color);
            transition: all 0.3s ease;
        }

        .otp-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 30px;
            background: linear-gradient(135deg, #059669, #047857);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 8px 25px rgba(5, 150, 105, 0.15);
        }

        .otp-icon svg {
            width: 40px;
            height: 40px;
            fill: white;
        }

        .otp-title {
            color: var(--text-main);
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 15px;
            letter-spacing: -0.5px;
        }

        .otp-subtitle {
            color: var(--subtitle-color);
            font-size: 16px;
            line-height: 1.6;
            margin-bottom: 5px;
        }

        .email-display {
            color: #2563eb;
            font-weight: 600;
            font-size: 16px;
            margin-bottom: 20px;
            word-break: break-all;
        }

        .otp-timer {
            color: var(--timer-text);
            font-weight: 600;
            font-size: 14px;
            margin-bottom: 20px;
            padding: 10px;
            background-color: var(--timer-bg);
            border-radius: 8px;
            border: 1px solid var(--timer-border);
            transition: all 0.3s ease;
        }

        .otp-timer.warning {
            animation: pulse 1s infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }

        .otp-input-container {
            display: flex;
            justify-content: center;
            gap: 12px;
            margin-bottom: 30px;
        }

        .otp-input {
            width: 55px;
            height: 55px;
            border: 2px solid var(--input-border);
            border-radius: 12px;
            text-align: center;
            font-size: 22px;
            font-weight: 700;
            color: var(--text-main);
            background-color: var(--input-bg);
            transition: all 0.3s ease;
        }

        .otp-input:focus {
            outline: none;
            border-color: #2563eb;
            background-color: var(--card-bg);
            box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
            transform: scale(1.05);
        }

        .otp-input.filled {
            border-color: var(--input-filled-border);
            background-color: var(--input-filled-bg);
        }

        .verify-button {
            width: 100%;
            padding: 16px 20px;
            background: linear-gradient(135deg, #2563eb, #1d4ed8);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(37, 99, 235, 0.2);
            margin-bottom: 25px;
            opacity: 0.6;
            pointer-events: none;
        }

        .verify-button.active {
            opacity: 1;
            pointer-events: auto;
        }

        .verify-button.active:hover {
            background: linear-gradient(135deg, #1d4ed8, #1e3a8a);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(37, 99, 235, 0.3);
        }

        .resend-container {
            margin-bottom: 25px;
            color: var(--subtitle-color);
            font-size: 14px;
        }

        .resend-button {
            color: #2563eb;
            background: none;
            border: none;
            cursor: pointer;
            font-size: 14px;
            font-weight: 600;
            text-decoration: underline;
            margin-left: 5px;
        }

        .resend-button:disabled {
            color: var(--subtitle-color);
            cursor: not-allowed;
            text-decoration: none;
        }

        .back-to-email {
            color: #2563eb;
            text-decoration: none;
            font-size: 14px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .error-message {
            background-color: var(--timer-bg);
            color: var(--timer-text);
            padding: 16px 20px;
            border-radius: 12px;
            margin-bottom: 25px;
            border: 1px solid var(--timer-border);
            display: none;
            font-size: 14px;
        }

        /* Responsive Styles */
        @media (max-width: 768px) {
            .otp-container { padding: 40px 30px; }
            .otp-input { width: 45px; height: 45px; font-size: 18px; }
            .otp-input-container { gap: 8px; }
        }

        @media (max-width: 480px) {
            .otp-container { padding: 30px 20px; }
            .otp-title { font-size: 24px; }
            .otp-input { width: 40px; height: 40px; }
        }
    </style>
</head>
<body>
    <div class="main-content">
        <div class="otp-container">
            <div class="otp-icon">
                <svg viewBox="0 0 24 24">
                    <path d="M20,8L12,13L4,8V6L12,11L20,6M20,4H4C2.89,4 2,4.89 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V6C22,4.89 21.1,4 20,4Z"/>
                </svg>
            </div>
            
            <h1 class="otp-title">Verify Your Email</h1>
            <p class="otp-subtitle">We've sent a 4-digit verification code to</p>
            <div class="email-display" id="emailDisplay">your email address</div>

            <div class="otp-timer" id="otpTimer">
                OTP expires in: <span id="timerText">60</span>s
            </div>

            <div class="error-message" id="errorMessage"></div>

            <form id="otpForm">
                <div class="otp-input-container">
                    <input type="text" class="otp-input" maxlength="1" data-index="0" inputmode="numeric">
                    <input type="text" class="otp-input" maxlength="1" data-index="1" inputmode="numeric">
                    <input type="text" class="otp-input" maxlength="1" data-index="2" inputmode="numeric">
                    <input type="text" class="otp-input" maxlength="1" data-index="3" inputmode="numeric">
                </div>

                <button type="submit" class="verify-button" id="verifyButton">
                    Verify Code
                </button>
            </form>

            <div class="resend-container">
                Didn't receive the code?
                <button type="button" class="resend-button" id="resendButton">Resend Code</button>
                <span class="countdown" id="countdown" style="display: none; color: #ef4444; font-weight: 600;"></span>
            </div>

            <a href="#" class="back-to-email" onclick="history.back()">← Back to Email</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        const otpInputs = document.querySelectorAll('.otp-input');
        const verifyButton = document.getElementById('verifyButton');
        const resendButton = document.getElementById('resendButton');
        const countdown = document.getElementById('countdown');
        const errorMessage = document.getElementById('errorMessage');
        const emailDisplay = document.getElementById('emailDisplay');
        const otpTimer = document.getElementById('otpTimer');
        const timerText = document.getElementById('timerText');

        let resendCountdown = 60;
        let otpExpiryCountdown = 60;
        let countdownInterval;
        let otpExpiryInterval;
        let otpExpired = false;

        const verificationEmail = sessionStorage.getItem('verificationEmail');
        if (verificationEmail) emailDisplay.textContent = verificationEmail;

        // OTP Input Logic
        otpInputs.forEach((input, index) => {
            input.addEventListener('input', e => {
                if (otpExpired) { e.target.value = ''; return; }
                const val = e.target.value.replace(/\D/g, '');
                e.target.value = val;
                e.target.classList.toggle('filled', !!val);
                if (val && index < otpInputs.length - 1) otpInputs[index + 1].focus();
                toggleVerifyButton();
            });

            input.addEventListener('keydown', e => {
                if (e.key === 'Backspace' && !e.target.value && index > 0) {
                    otpInputs[index - 1].focus();
                }
            });
        });

        function toggleVerifyButton() {
            const allFilled = Array.from(otpInputs).every(input => input.value !== '');
            verifyButton.classList.toggle('active', allFilled && !otpExpired);
        }

        function startOtpExpiryTimer() {
            otpExpiryCountdown = 60;
            otpExpired = false;
            otpTimer.classList.remove('warning');
            otpTimer.innerHTML = 'OTP expires in: <span id="timerText">60</span>s';
            const tText = document.getElementById('timerText');

            clearInterval(otpExpiryInterval);
            otpExpiryInterval = setInterval(() => {
                otpExpiryCountdown--;
                if (tText) tText.textContent = otpExpiryCountdown;

                if (otpExpiryCountdown <= 15) otpTimer.classList.add('warning');

                if (otpExpiryCountdown <= 0) {
                    clearInterval(otpExpiryInterval);
                    otpExpired = true;
                    otpInputs.forEach(i => i.disabled = true);
                    verifyButton.classList.remove('active');
                    otpTimer.innerHTML = 'OTP has <span style="color: #ef4444;">expired</span>';
                    errorMessage.textContent = '⏰ Your OTP has expired. Please request a new one.';
                    errorMessage.style.display = 'block';
                }
            }, 1000);
        }

        function startResendCountdown() {
            resendCountdown = 60;
            resendButton.disabled = true;
            countdown.style.display = 'inline';
            
            clearInterval(countdownInterval);
            countdownInterval = setInterval(() => {
                resendCountdown--;
                countdown.textContent = `(${resendCountdown}s)`;
                if (resendCountdown <= 0) {
                    clearInterval(countdownInterval);
                    countdown.style.display = 'none';
                    resendButton.disabled = false;
                }
            }, 1000);
        }

        document.getElementById('otpForm').addEventListener('submit', async e => {
            e.preventDefault();
            const otp = Array.from(otpInputs).map(i => i.value).join('');
            
            try {
                const res = await fetch('/forgotPassword-otp', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ otp })
                });
                const data = await res.json();
                if (data.success) {
                    Swal.fire({ icon: 'success', title: 'Verified!', showConfirmButton: false, timer: 1500 })
                        .then(() => window.location.href = data.redirectUrl);
                } else {
                    Swal.fire({ icon: 'error', title: 'Invalid OTP', text: data.message });
                }
            } catch (err) {
                Swal.fire({ icon: 'error', title: 'Error', text: 'Server error, try again.' });
            }
        });

        resendButton.addEventListener('click', async () => {
            startResendCountdown();
            startOtpExpiryTimer();
            otpInputs.forEach(i => { i.disabled = false; i.value = ''; i.classList.remove('filled'); });
            errorMessage.style.display = 'none';

            await fetch('/resend-forgot-otp', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: verificationEmail })
            });
        });

        window.onload = () => {
            startOtpExpiryTimer();
            startResendCountdown();
            otpInputs[0].focus();
        };
    </script>
</body>
</html>

<%- include("../partials/user/footer.ejs") %>