<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sentique Login</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: #ffffff;
        }
        .container {
            display: flex;
            width: 80%;
            max-width: 1100px;
            box-shadow: 0 0 15px rgba(8, 8, 8, 0.1);
            background: #7e9bb1;
            border-radius: 10px;
             overflow: hidden;
        }
        
.left-section {
    flex: 1;
    min-height: 500px;
    display: flex;
    justify-content: center;
    align-items: center;
    background-color: #000; /* fallback */
}

.left-section img {
    width: 100%;
    height: 500px;
    object-fit:fill
}
       
        .right-section {
            flex: 1;
            padding: 50px;
        }
        h1 {
            font-size: 28px;
            font-weight: bold;
        }
        p {
            color: #555;
        }
        input {
            width: 100%;
            padding: 10px;
            margin: 15px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        .login-btn {
            width: 100%;
            background: #034456;
            color: white;
            padding: 12px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
        }
        .google-btn {
            width: 100%;
            padding: 12px;
            border: 1px solid #ccc;
            background: white;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            font-size: 14px;
            margin-top: 15px;
            cursor: pointer;
            border-radius: 5px;
        }
        .link {
            text-align: center;
            margin-top: 20px;
            font-size: 14px;
        }
        .link a {
            color: #333;
            text-decoration: none;
            font-weight: bold;
        }
        .forgot {
            text-align: right;
            margin: -10px 0 10px 0;
        }
        .forgot a {
            color: #3a0202;
            font-size: 14px;
            text-decoration: none;
        }
        .alert-danger {
            color: red;
            margin: 10px 0;
            font-weight: bold;} 
     

        /* Responsive */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                width: 95%;
            }

            .left-section {
                min-height: 300px;
            }

            .right-section {
                padding: 30px 20px;
            }
        }
    </style>
</head>
<body>



<div class="container">
  <div class="left-section">
    <img src="/photos/signup-cover image.png" alt="Signup Cover Image" />
  </div>
  <div class="right-section">
    <h1>Log in to Sentique</h1>
    <p>Enter your details below</p>

    <form action="/login" method="POST">
      <input type="email" name="email" placeholder="Email" required>
      <input type="password" name="password" placeholder="Password" required>

      <% if (message) { %>
        <div class="alert alert-danger text-center">
           <%= message %>
        </div>
     <% } %>


      <div class="forgot">
        <a href="/forgotpassword">Forgot Password?</a>
      </div>

      <button type="submit" class="login-btn">Login</button>
    </form>

    <form action="/auth/google" method="GET">
      <button type="submit" class="google-btn">
        <img src="https://img.icons8.com/color/16/000000/google-logo.png"/>
        Sign In with Google
      </button>
    </form>

    <div class="link">
      Don't have account? <a href="/signup">Sign Up</a>
    </div>
  </div>
</div>


</body>
</html>
