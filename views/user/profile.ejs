
<%- include("../partials/user/header.ejs") %>
<head>
  <title>My Account - <%= user.name %></title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

 <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            color: #2d3748;
        }

        /* ========== MAIN CONTENT ========== */
        .profile-section {
            padding: 40px 20px;
        }

        .container {
            max-width: 1280px;
            margin: 0 auto;
        }

        .breadcrumb {
            display: flex;
            gap: 10px;
            margin-bottom: 30px;
            font-size: 14px;
            color: #718096;
            flex-wrap: wrap;
        }

        .breadcrumb a {
            color: #4a5568;
            text-decoration: none;
            transition: color 0.3s;
        }

        .breadcrumb a:hover {
            color: #667eea;
        }

        .welcome-header {
            background: white;
            padding: 25px 30px;
            border-radius: 15px;
            margin-bottom: 30px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .welcome-header h1 {
            font-size: 28px;
            color: #2d3748;
            font-weight: 700;
        }

        .welcome-header span {
            color: #3f979a;
            font-weight: 600;
        }

        .main-content {
            display: grid;
            grid-template-columns: 280px 1fr;
            gap: 30px;
        }

        /* ========== SIDEBAR ========== */
        .sidebar {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
            height: fit-content;
        }

        .profile-avatar-container {
            text-align: center;
            margin-bottom: 20px;
        }

        .profile-avatar {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            background: linear-gradient(135deg, #141a31 0%, #18342f 100%);
            margin: 0 auto 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 40px;
            font-weight: 600;
            box-shadow: 0 8px 20px rgba(102, 126, 234, 0.3);
            position: relative;
            overflow: hidden;
        }

        .profile-avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .profile-avatar-placeholder {
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .upload-btn-wrapper {
            position: relative;
            display: inline-block;
            margin-top: 10px;
        }

        .upload-btn {
            padding: 8px 20px;
            background: #667eea;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 500;
            font-size: 13px;
            transition: all 0.3s;
        }

        .upload-btn:hover {
            background: #5568d3;
            transform: translateY(-2px);
        }

        .upload-btn-wrapper input[type=file] {
            font-size: 100px;
            position: absolute;
            left: 0;
            top: 0;
            opacity: 0;
            cursor: pointer;
        }

        .profile-name {
            text-align: center;
            font-size: 18px;
            font-weight: 600;
            color: #2d3748;
            margin-bottom: 30px;
        }

        .menu-section {
            margin-bottom: 25px;
        }

        .menu-title {
            font-size: 11px;
            text-transform: uppercase;
            color: #a0aec0;
            font-weight: 600;
            margin-bottom: 12px;
            letter-spacing: 1px;
        }

        .menu-item {
            padding: 12px 15px;
            margin-bottom: 5px;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s;
            color: #4a5568;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 14px;
            text-decoration: none;
        }

        .menu-item:hover {
            background: #f7fafc;
            color: #2f656e;
            transform: translateX(5px);
        }

        .menu-item.active {
            background: linear-gradient(135deg, #183043 0%, #10262c 100%);
            color: white;
        }

        /* ========== PROFILE CARD ========== */
        .profile-card {
            background: white;
            border-radius: 15px;
            padding: 35px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 35px;
            padding-bottom: 20px;
            border-bottom: 2px solid #f7fafc;
            flex-wrap: wrap;
            gap: 15px;
        }

        .card-title {
            font-size: 24px;
            font-weight: 700;
            color: #2d3748;
        }

        .tab-group {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
        }

        .tab {
            padding: 10px 0;
            cursor: pointer;
            color: #718096;
            font-weight: 500;
            border-bottom: 3px solid transparent;
            transition: all 0.3s;
            font-size: 15px;
        }

        .tab.active {
            color: #667eea;
            border-bottom-color: #667eea;
        }

        /* ========== TAB CONTENT ========== */
        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: block;
        }

        /* ========== FORM ========== */
        .form-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 25px;
            margin-bottom: 25px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
        }

        .form-group.full-width {
            grid-column: 1 / -1;
        }

        .form-label {
            font-size: 14px;
            font-weight: 600;
            color: #4a5568;
            margin-bottom: 8px;
        }

        .form-input, .form-select {
            padding: 14px 18px;
            border: 2px solid #e2e8f0;
            border-radius: 10px;
            font-family: 'Inter', sans-serif;
            font-size: 15px;
            transition: all 0.3s;
            background: #f9fafb;
        }

        .form-input:focus, .form-select:focus {
            outline: none;
            border-color: #667eea;
            background: white;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .form-input:disabled {
            background: #e2e8f0;
            cursor: not-allowed;
        }

        /* ========== RADIO GROUP ========== */
        .radio-group {
            display: flex;
            gap: 25px;
            padding: 14px 0;
        }

        .radio-option {
            display: flex;
            align-items: center;
            gap: 8px;
            cursor: pointer;
        }

        .radio-option input[type="radio"] {
            width: 20px;
            height: 20px;
            cursor: pointer;
        }

        .radio-option label {
            cursor: pointer;
            font-size: 15px;
            color: #4a5568;
        }

        /* ========== REFERRAL BOX ========== */
        .referral-box {
            background: linear-gradient(135deg, #fef5e7 0%, #fdebd0 100%);
            padding: 20px;
            border-radius: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 25px;
            gap: 15px;
            flex-wrap: wrap;
        }

        .referral-text {
            color: #7c3aed;
            font-weight: 600;
            font-size: 14px;
        }

        .referral-code {
            display: flex;
            gap: 10px;
            align-items: center;
            flex-wrap: wrap;
        }

        .code-display {
            background: white;
            padding: 12px 20px;
            border-radius: 8px;
            font-weight: 700;
            color: #7c3aed;
            letter-spacing: 2px;
            font-size: 14px;
        }

        .copy-btn {
            padding: 12px 25px;
            background: #7c3aed;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s;
            font-size: 14px;
        }

        .copy-btn:hover {
            background: #6d28d9;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(124, 58, 237, 0.3);
        }

        .copy-btn:active {
            transform: translateY(0);
        }

        /* ========== SECURITY SECTION ========== */
        .security-section {
            padding: 30px;
            background: #f9fafb;
            border-radius: 12px;
            border: 1px solid #e2e8f0;
        }

        .section-subtitle {
            font-size: 18px;
            font-weight: 600;
            color: #2d3748;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 2px solid #e2e8f0;
        }

        /* ========== BUTTON ========== */
        .button-group {
            display: flex;
            justify-content: flex-end;
            gap: 15px;
            margin-top: 30px;
            flex-wrap: wrap;
        }

        .update-btn {
            padding: 16px 40px;
            background: linear-gradient(135deg, #619141 0%, #488e42 100%);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }

        .update-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(102, 126, 234, 0.4);
        }

        .update-btn:active {
            transform: translateY(0);
        }

        .cancel-btn {
            padding: 16px 40px;
            background: white;
            color: #667eea;
            border: 2px solid #667eea;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }

        .cancel-btn:hover {
            background: #f7fafc;
        }

        /* ========== ALERT MESSAGES ========== */
        .alert {
            padding: 15px 20px;
            border-radius: 10px;
            margin-bottom: 20px;
            font-weight: 500;
        }

        .alert-success {
            background: #d1fae5;
            color: #065f46;
            border: 1px solid #6ee7b7;
        }

        .alert-error {
            background: #fee2e2;
            color: #991b1b;
            border: 1px solid #fca5a5;
        }

        /* ========== RESPONSIVE DESIGN ========== */
        @media (max-width: 1024px) {
            .main-content {
                grid-template-columns: 250px 1fr;
                gap: 25px;
            }

            .profile-card {
                padding: 30px;
            }

            .welcome-header h1 {
                font-size: 24px;
            }
        }

        @media (max-width: 868px) {
            .main-content {
                grid-template-columns: 1fr;
                gap: 20px;
            }

            .sidebar {
                display: grid;
                grid-template-columns: auto 1fr;
                gap: 30px;
                align-items: start;
            }

            .sidebar-left {
                display: flex;
                flex-direction: column;
                align-items: center;
            }

            .sidebar-menu {
                width: 100%;
            }

            .profile-avatar {
                width: 80px;
                height: 80px;
                font-size: 32px;
            }

            .profile-name {
                font-size: 16px;
                margin-bottom: 0;
            }

            .menu-section {
                margin-bottom: 20px;
            }

            .form-grid {
                grid-template-columns: 1fr;
                gap: 20px;
            }

            .card-title {
                font-size: 22px;
            }
        }

        @media (max-width: 640px) {
            .profile-section {
                padding: 30px 15px;
            }

            .sidebar {
                grid-template-columns: 1fr;
                text-align: center;
                padding: 25px;
            }

            .sidebar-left {
                width: 100%;
            }

            .menu-item {
                justify-content: center;
            }

            .welcome-header {
                padding: 20px;
                flex-direction: column;
                text-align: center;
                gap: 10px;
            }

            .welcome-header h1 {
                font-size: 20px;
            }

            .profile-card {
                padding: 25px 20px;
            }

            .card-header {
                flex-direction: column;
                align-items: flex-start;
            }

            .tab-group {
                width: 100%;
                justify-content: space-between;
            }

            .card-title {
                font-size: 20px;
            }

            .referral-box {
                flex-direction: column;
                text-align: center;
                padding: 20px 15px;
            }

            .referral-code {
                justify-content: center;
            }

            .button-group {
                flex-direction: column-reverse;
                width: 100%;
            }

            .update-btn,
            .cancel-btn {
                width: 100%;
                padding: 14px;
            }

            .radio-group {
                flex-direction: column;
                gap: 15px;
            }
        }

        @media (max-width: 480px) {
            .profile-avatar {
                width: 70px;
                height: 70px;
                font-size: 28px;
            }

            .form-input, .form-select {
                padding: 12px 15px;
                font-size: 14px;
            }

            .form-label {
                font-size: 13px;
            }

            .tab {
                font-size: 14px;
            }

            .referral-text {
                font-size: 13px;
            }

            .code-display {
                padding: 10px 15px;
                font-size: 13px;
            }
        }
    </style>

</head>
<body>


  <section class="profile-section">
    <div class="container">
      <% if (success_msg && success_msg.length > 0) { %>
        <div class="alert alert-success"><%= success_msg %></div>
      <% } %>
      <% if (error_msg && error_msg.length > 0) { %>
        <div class="alert alert-error"><%= error_msg %></div>
      <% } %>

      <div class="breadcrumb">
        <a href="/">Home</a> / <a href="/profile">My Account</a> / <span>Profile</span>
      </div>

      <div class="welcome-header">
        <h1>Welcome! <span><%= user.name %></span></h1>
      </div>

      <div class="main-content">
        <aside class="sidebar">
          <div class="sidebar-left">
            <div class="profile-avatar-container">
                <div class="profile-avatar">
  <% if (user.image) { %>
    <img src="<%= user.image %>" alt="Profile Picture">
  <% } else { %>
    <img src="/images/default-profile.png" alt="Default Picture">
  <% } %>
</div>

              
             
            </div>
            <div class="profile-name"><%= user.name %></div>
          </div>

          <div class="sidebar-menu">
            <div class="menu-section">
              <div class="menu-title">Account</div>
              <a href="/profile" class="menu-item active">
                <span>👤</span> My Profile
              </a>
              <a href="/profile/address" class="menu-item">
                <span>📍</span> Address Book
              </a>
            </div>
            <div class="menu-section">
              <div class="menu-title">Shopping</div>
              <a href="/profile/order-list" class="menu-item">
                <span>📦</span> My Orders
              </a>
              <a href="/wishlist" class="menu-item">
                <span>❤️</span> My Wishlist
              </a>
              <a href="/cart" class="menu-item">
                <span>🛒</span> Shopping Cart
              </a>
            </div>
            <div class="menu-section">
              <div class="menu-title">Payments</div>
              <a href="/wallet" class="menu-item">
                <span>💳</span> Wallet
              </a>
              <a href="/coupons" class="menu-item">
                <span>🎟️</span> Coupons
              </a>
            </div>
          </div>
        </aside>

        <main>
          <div class="profile-card">
            <div class="card-header">
              <h2 class="card-title">Account Settings</h2>
              <div class="tab-group">
                <div class="tab active" data-tab="profile">Personal Information</div>
                <div class="tab" data-tab="security">Security</div>
              </div>
            </div>

        <div class="tab-content active" id="profile-tab">
  <div class="form-grid">
    <div class="form-group">
      <label class="form-label">Full Name</label>
      <input type="text" class="form-input" value="<%= user.name %>" disabled>
    </div>
    <div class="form-group">
      <label class="form-label">Phone</label>
      <input type="tel" class="form-input" value="<%= user.phone || '' %>" disabled>
    </div>
    <div class="form-group full-width">
      <label class="form-label">Email</label>
      <input type="email" class="form-input" value="<%= user.email %>" disabled>
    </div>
  </div>

  <% if (user.referralCode) { %>
  <div class="referral-box">
    <span class="referral-text">Share your code with friends and earn rewards!</span>
    <div class="referral-code">
      <span class="code-display"><%= user.referralCode %></span>
      <button type="button" class="copy-btn" onclick="copyCode('<%= user.referralCode %>')">COPY</button>
    </div>
  </div>
  <% } %>

  <div class="button-group">
    <a href="/profile-edit" class="update-btn">Edit Profile</a>
  </div>
</div>

<div class="tab-content" id="security-tab">
  <!-- Change Password -->
  <div class="security-section">
    <h3 class="section-subtitle">Change Password</h3>
    <form id="passwordForm">
      <div class="form-grid">
        <div class="form-group full-width">
          <label class="form-label">Current Password</label>
          <input type="password" name="currentPassword" class="form-input" placeholder="Enter current password" required>
        </div>
        <div class="form-group">
          <label class="form-label">New Password</label>
          <input type="password" name="newPassword" class="form-input" placeholder="Enter new password" required minlength="6">
        </div>
        <div class="form-group">
          <label class="form-label">Confirm Password</label>
          <input type="password" name="confirmPassword" class="form-input" placeholder="Confirm new password" required minlength="6">
        </div>
      </div>

      <div class="button-group">
        <button type="button" class="cancel-btn" onclick="document.getElementById('passwordForm').reset()">Cancel</button>
        <button type="submit" class="update-btn">Change Password</button>
      </div>
    </form>
  </div>

  <!-- Change Email -->
  <div class="security-section" style="margin-top: 40px;">
    <h3 class="section-subtitle">Change Email Address</h3>
    <form id="emailForm">
      <div class="form-grid">
        <div class="form-group full-width">
          <label class="form-label">Current Email</label>
          <input type="email" class="form-input" value="<%= user.email %>" disabled>
        </div>
        <div class="form-group full-width">
          <label class="form-label">New Email Address</label>
          <input type="email" name="newEmail" id="newEmail" class="form-input" placeholder="Enter new email address" required>
        </div>
        <div class="form-group full-width">
          <label class="form-label">Confirm Password</label>
          <input type="password" name="password" id="emailPassword" class="form-input" placeholder="Enter your password" required>
        </div>
      </div>

      <div class="button-group">
        <button type="button" class="cancel-btn" onclick="document.getElementById('emailForm').reset()">Cancel</button>
        <button type="button" id="sendOtpBtn" class="update-btn">Send OTP</button>
      </div>
    </form>
  </div>
</div>

          </div>
        </main>
      </div>
    </div>
  </section>



  <script>
    function copyCode(code) {
      const btn = event.target;
      navigator.clipboard.writeText(code).then(() => {
        const originalText = btn.textContent;
        btn.textContent = "COPIED!";
        btn.style.background = "#10b981";
        setTimeout(() => {
          btn.textContent = originalText;
          btn.style.background = "#7c3aed";
        }, 2000);
      }).catch(err => {
        console.error("Failed to copy:", err);
        Swal.fire({ icon: "error", title: "Error", text: "Failed to copy code" });
      });
    }

    const tabs = document.querySelectorAll(".tab");
    const contents = document.querySelectorAll(".tab-content");

    const lastTab = localStorage.getItem("activeTab");
    if (lastTab) {
      tabs.forEach(t => t.classList.remove("active"));
      contents.forEach(c => c.classList.remove("active"));
      document.querySelector(`.tab[data-tab="${lastTab}"]`).classList.add("active");
      document.getElementById(`${lastTab}-tab`).classList.add("active");
    }

    tabs.forEach(tab => {
      tab.addEventListener("click", function () {
        const tabName = this.dataset.tab;
        localStorage.setItem("activeTab", tabName);
        tabs.forEach(t => t.classList.remove("active"));
        contents.forEach(c => c.classList.remove("active"));
        this.classList.add("active");
        document.getElementById(tabName + "-tab").classList.add("active");
      });
    });

    document.querySelectorAll(".menu-item").forEach(item => {
      item.addEventListener("click", function () {
        document.querySelectorAll(".menu-item").forEach(i => i.classList.remove("active"));
        this.classList.add("active");
      });
    });

    function validatePassword(password) {
      const regex = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[\W_]).{6,}$/;
      return regex.test(password);
    }

    function validateEmail(email) {
      const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      return regex.test(email);
    }

    document.getElementById("passwordForm").addEventListener("submit", async e => {
      e.preventDefault();
      const formData = new FormData(e.target);
      const body = Object.fromEntries(formData.entries());
      body.currentPassword = body.currentPassword.trim();
      body.newPassword = body.newPassword.trim();
      body.confirmPassword = body.confirmPassword.trim();

      if (!validatePassword(body.newPassword)) {
        Swal.fire({ icon: "error", title: "Invalid Password", text: "Password must be at least 6 characters and include uppercase, lowercase, number, and special character." });
        return;
      }

      if (body.newPassword !== body.confirmPassword) {
        Swal.fire({ icon: "error", title: "Mismatch", text: "New password and confirm password do not match." });
        return;
      }

      try {
        Swal.fire({ title: "Initiating Change...", allowOutsideClick: false, didOpen: () => Swal.showLoading() });
        const res = await fetch("/profile/change-password-init", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify(body),
        });
        Swal.close();

        if (!res.ok) {
          const text = await res.text();
          console.error("Response:", res.status, text);
          throw new Error("Server responded with status: " + res.status);
        }

        const data = await res.json();
        if (data.success && data.token && data.type) {
          Swal.fire({ icon: "success", title: "OTP Sent", text: data.message, timer: 1500 })
            .then(() => {
              window.location.href = `/profile/change-email-otp?type=${data.type}&token=${data.token}`;
            });
        } else {
          Swal.fire({ icon: "error", title: "Error", text: data.message || "Failed to initiate password change" });
        }
      } catch (err) {
        console.error("Fetch error:", err);
        Swal.fire({ icon: "error", title: "Error", text: "Failed to initiate password change: " + err.message });
      }
    });

    document.getElementById("sendOtpBtn").addEventListener("click", async () => {
      const newEmail = document.getElementById("newEmail").value.trim();
      const password = document.getElementById("emailPassword").value.trim();

      if (!newEmail || !password) {
        Swal.fire({ icon: "error", title: "Missing Fields", text: "Please enter new email and password!" });
        return;
      }

      if (!validateEmail(newEmail)) {
        Swal.fire({ icon: "error", title: "Invalid Email", text: "Please enter a valid email address!" });
        return;
      }

      try {
        Swal.fire({ title: "Sending OTP...", allowOutsideClick: false, didOpen: () => Swal.showLoading() });
        const res = await fetch("/profile/change-email-init", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ newEmail, password }),
        });
        Swal.close();

        if (!res.ok) {
          const text = await res.text();
          console.error("Response:", res.status, text);
          throw new Error("Server responded with status: " + res.status);
        }

        const data = await res.json();
        if (data.success && data.token && data.type) {
          Swal.fire({ icon: "success", title: "OTP Sent", text: data.message, timer: 1500 })
            .then(() => {
              window.location.href = `/profile/change-email-otp?type=${data.type}&token=${data.token}`;
            });
        } else {
          Swal.fire({ icon: "error", title: "Error", text: data.message || "Failed to send OTP" });
        }
      } catch (err) {
        console.error("Fetch error:", err);
        Swal.fire({ icon: "error", title: "Error", text: "Failed to send OTP: " + err.message });
      }
    });
  </script>
</body>
<%- include("../partials/user/footer.ejs") %>




