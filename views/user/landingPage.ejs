

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sentique – Luxury Zodiac Perfumes</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Montserrat:wght@300;400;500&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      padding: 0;
      background: #0d0d0d;
      color: #fff;
      font-family: 'Montserrat', sans-serif;
    }

    h1, h2, h3 {
      font-family: 'Playfair Display', serif;
      margin: 0;
    }

    
    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 50px;
      background: rgba(10, 10, 10, 0.95);
      position: sticky;
      top: 0;
      z-index: 1000;
    }
    .logo img {
      height: 110px; 
    }
    nav {
      flex: 1;
      text-align: center;
    }
    nav a {
      margin: 0 15px;
      text-decoration: none;
      color: #ccc;
      font-weight: 500;
      transition: color 0.3s;
    }
    nav a:hover {
      color: gold;
    }

    
    .btn-small {
      background: gold;
      color: #000;
      padding: 8px 16px;
      border-radius: 20px;
      text-decoration: none;
      font-weight: bold;
      font-size: 0.9rem;
      transition: background 0.3s;
    }
    .btn-small:hover {
      background: #d4af37;
    }

    
    .user-section {
      display: flex;
      align-items: center;
      gap: 15px;
      position: relative;
    }
    .user-icon {
      width: 35px;
      height: 35px;
      border-radius: 50%;
      background: gold;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      font-size: 18px; 
      color: #000;
    }
    .dropdown {
      position: absolute;
      top: 50px;
      right: 0;
      background: #1a1a1a;
      border-radius: 10px;
      display: none;
      flex-direction: column;
      min-width: 150px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.5);
    }
    .dropdown a {
      padding: 10px;
      color: #ccc;
      text-decoration: none;
      transition: background 0.3s;
    }
    .dropdown a:hover {
      background: #333;
      color: gold;
    }
   

    
    .hero {
      text-align: center;
      padding: 100px 20px;
      background: #0a0a0a;
      position: relative;
    }
    .hero h1 {
      font-size: 5rem;
      letter-spacing: 3px;
      font-weight: 700;
    }
    .hero-sub {
      margin: 10px 0 40px;
      font-size: 1.2rem;
      color: #cfcfcf;
    }
    .hero-products {
      display: flex;
      justify-content: center;
      gap: 50px;
      flex-wrap: wrap;
    }
    .hero-products img {
      width: 220px;
      height: auto;
      border-radius: 10px;
      background: #1a1a1a;
      padding: 10px;
    }

   
    .section {
      padding: 80px 20px;
      text-align: center;
    }
    .section h2 {
      font-size: 3rem;
      margin-bottom: 20px;
    }
    .section p {
      color: #b5b5b5;
      max-width: 700px;
      margin: 0 auto 40px;
    }


    .product-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 30px;
      margin-top: 40px;
    }
    .product-card {
      background: #1a1a1a;
      padding: 20px;
      border-radius: 15px;
      text-align: center;
      transition: transform 0.3s;
    }
    .product-card:hover {
      transform: translateY(-10px);
    }
    .product-card img {
      max-width: 100%;
      border-radius: 10px;
      margin-bottom: 15px;
    }
    .product-card h3 {
      font-size: 1.5rem;
      margin-bottom: 10px;
    }
    .product-card p {
      font-size: 1rem;
      color: #ccc;
    }

   
    .cta {
      background: linear-gradient(to right, #111, #222);
      text-align: center;
      padding: 80px 20px;
    }
    .cta h2 {
      font-size: 2.5rem;
      margin-bottom: 20px;
    }
    .btn {
      background: gold;
      color: #000;
       text-decoration: none; 
      padding: 12px 30px;
      border: none;
      border-radius: 30px;
      font-weight: bold;
      cursor: pointer;
      font-size: 1rem;
    }
    .btn:hover {
  background-color: #ff6333; 
}

  
    footer {
      text-align: center;
      padding: 30px;
      background: #0a0a0a;
      color: #888;
      font-size: 0.9rem;
    }
  </style>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

</head>
<body>

<header>
  <a href="#" class="logo"><img src="/photos/zodiac perfume/brand logo white.png" alt="Sentique Logo"></a>

  <nav>
    <a href="/home">Home</a>
    <a href="/shopPage">Collections</a>
    <a href="#">About</a>
    <a href="#">Contact</a>
  </nav>

  <div class="user-section">
    <% if (!user) { %>
      <!-- If NOT logged in -->
      <a href="/login" class="btn-small">Login</a>
      <a href="/signup" class="btn-small">Signup</a>
    <% } else { %>
      <!-- If logged in -->
      <div class="user-icon" id="userIcon">
        <i class="fas fa-user"></i>
      </div>
      <span><%= user.name %></span> <!-- Show username -->
      <div class="dropdown" id="userDropdown">
        <a href="/profile">My Profile</a>
        <a href="/orders">My Orders</a>
        <a href="/logout">Logout</a>
      </div>
    <% } %>
  </div>
</header>


  <section class="hero">
    <h1>SENTIQUE</h1>
    <p class="hero-sub">Exclusive Scents, Inspired by the Stars</p>
    <div class="hero-products">
      <img src="/photos/landing-images/landing-img2.0.png" alt="Perfume 1">
      <img src="/photos/landing-images/landing-img3.png" alt="Perfume 2">
      <img src="/photos/landing-images/landing-img2.png" alt="Perfume 3">
      
    </div>
  </section>

  
  <section class="section">
    <h2>Exclusive Scents</h2>
    <p>Crafted with passion and aligned with the zodiac, Sentique perfumes embody the essence of your star sign.</p>
    <div class="product-grid">
      <div class="product-card">
        <img src="/photos/landing-images/landing-img4.png" alt="Perfume">
        <h3>Neonitre</h3>
        <p>Bold and timeless fragrance</p>
      </div>
      <div class="product-card">
        <img src="/photos/landing-images/landing-img3.png" alt="Perfume">
        <h3>Lovet</h3>
        <p>Romantic notes with elegance</p>
      </div>
      <div class="product-card">
        <img src="/photos/landing-images/landing-img1.png" alt="Perfume">
        <h3>Libuos</h3>
        <p>Mystical scent of luxury</p>
      </div>
    </div>
  </section>

 
  <section class="cta">
    <h2>Your Zodiac. Your Scent. Your Signature.</h2>
    <a class="btn" href="/home">Shop Now</a>
  </section>

  
  <footer>
    <p>&copy; 2025 Sentique – Scent Your Signature</p>
  </footer>


  <script>
  const userIcon = document.getElementById("userIcon");
  const userDropdown = document.getElementById("userDropdown");

  userIcon.addEventListener("click", () => {
    userDropdown.style.display =
      userDropdown.style.display === "flex" ? "none" : "flex";
  });

  
  document.addEventListener("click", (e) => {
    if (!userIcon.contains(e.target) && !userDropdown.contains(e.target)) {
      userDropdown.style.display = "none";
    }
  });
</script>

</body>
</html>
